import MainClient from "./page-client";

/**
 * Main component
 * @returns {JSX.Element}
 * @author Ahmed Emad Nasr
 * @description A functional server component that renders the client-side page wrapper.
 */
export default function Main() {
  return <MainClient />;
}